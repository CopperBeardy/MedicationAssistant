@page "/prescriptions"


<h3>Prescriptions </h3>

@*<DxListBox Data="@Prescriptions"
TextFieldName="@nameof(Prescription.CollectedOn)"
ValueFieldName="@nameof(Prescription.PrescriptionItems)"
SelectionMode="ListBoxSelectionMode.Single"
@bind-Values="@Values">
   
</DxListBox>*@

<DxDataGrid Data="@Prescriptions"
            RowRemovingAsync="@((dataItem) => OnRowRemoving(dataItem))"
            RowUpdatingAsync="@((updatingDataItem, newValues) => OnRowUpdating(updatingDataItem,(Dictionary<string,object>) newValues))"
            RowInsertingAsync="@((newValues) => OnRowInserting((Dictionary<string,object>)newValues))"
            CssClass="mw-1100"
            SelectionMode="DataGridSelectionMode.None"
            AutoCollapseDetailRow="true"
            ShowDetailRow="true">

    <DxDataGridCommandColumn Width="150px"></DxDataGridCommandColumn>
    <DxDataGridDateEditColumn Field="@nameof(Prescription.CollectedOn)"  Caption="Collected On" />
     
      
   
</DxDataGrid>